<%= simple_form_for @link do |f| %>
  <%= f.input :original_link, label: false, placeholder: "Put your link here" %>
  <%= f.submit "Create a shortened link", class: "btn btn-block btn-primary" %>

<% end %>
